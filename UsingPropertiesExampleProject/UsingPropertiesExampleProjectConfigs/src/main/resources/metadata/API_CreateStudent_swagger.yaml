openapi: 3.0.1
info:
  title: API_CreateStudent
  description: API Definition of API_CreateStudent
  version: 1.0.0
servers:
- url: http://localhost:8290/wso2/integration/students
paths:
  /:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
